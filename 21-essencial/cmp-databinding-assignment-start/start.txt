Bind it
1. Create three new componets GameControl,Odd and Even
2. The GameControl componet shoul have buttons to start and stop de GameControl
3. When starting the game, an evenet (Holding a incrementing number) should get emitted each second(ref=setInterval())
4. The event should be listenable from outside the component
5. When stopping the, no more avents should get emitted (clearInterval(ref))
6. A new Odd component should get created for every odd number emitted the same should happen for the even componet (on even number)
7. Simply output Odd- NUMBER OR Even -NUMBER in the two componets
8. Style the element (e.r paragraph) holding you output text differently in both componets